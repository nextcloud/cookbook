diff --git a/.github/workflows/sync-workflow-templates.yml b/.github/workflows/sync-workflow-templates.yml
index 40d39e1f..089a0844 100644
--- a/.github/workflows/sync-workflow-templates.yml
+++ b/.github/workflows/sync-workflow-templates.yml
@@ -26,9 +26,6 @@ jobs:
       matrix:
         branches:
           - ${{ github.event.repository.default_branch }}
-          - 'stable35'
-          - 'stable34'
-          - 'stable33'
 
     name: Update workflows in ${{ matrix.branches }}
 
@@ -130,7 +127,7 @@ jobs:
           path: target
           signoff: true
           branch: 'automated/noid/${{ matrix.branches }}-update-workflows'
-          title: '[${{ matrix.branches }}] ci(actions): Update workflow templates from organization template repository'
+          title: 'ci(actions): Update workflow templates from organization template repository [${{ matrix.branches }}]'
           draft: ${{ env.DRAFT_ONLY == 1 }}
           add-paths: .github/workflows/*.yml,.github/actions-lock.txt
           body: |
@@ -138,4 +135,4 @@ jobs:
             ${{ env.SUMMARY }}
           labels: |
             dependencies
-            3. to review
+
